# 報告年齡基準日改為採檢日（DateSampled）— 修改說明

> 修改日期：2026-08-19　　分支：`fix/age-basis-datesampled`　　commit：`0c2ae1b`
> 狀態：**已實機驗證、已部署至 LIMS**

---

## 一、問題現象

客戶報告重出時，**實際年齡**與「您的生理年齡比實際年齡老／年輕 X 歲」這句話的數字會變動。同一份報告隔幾個月再印一次，差值就不一樣。

原因是報告渲染時才用 `new Date()`（開報告當下）重算實際年齡：

```js
// 改前
const ageString = computeAgeByDOB(dob);   // 沒帶 endDate → 落到 new Date()
```

### 釐清：生理年齡本身不會漂移

這點容易誤解。**生理年齡是後端存下來的 `predictedValue`**（迴歸模型的連續輸出），報告只是讀資料庫的值、截小數 1 位顯示，重出幾次都一樣。

會漂移的是它拿去比較的**實際年齡**，以及因此連帶變動的差值敘述。

---

## 二、為什麼選採檢日（DateSampled）

因為 **R 後端餵給模型的實際年齡本來就是採檢日算的**。

`OmicsDP/R/class_senaite_sdk.R:134-137`

```r
val <- as.integer(difftime(as.Date(res$items[[1]]$getDateSampled),
                           as.Date(res$items[[1]]$getDateOfBirth),
                           units = "days")) / 365.25
val <- round(val, 3)
```

採檢日減出生日的**天數 ÷ 365.25**（不是月份）。前端若用「今天」，等於拿一個跟模型輸入不同基準的年齡去跟模型輸出比較。

三個候選基準日的取捨：

| 基準日 | 結果 |
|---|---|
| 報告日（改前行為） | 重出就漂移 ❌ |
| 收樣日 `DateReceived` | 穩定，但與模型輸入不一致 |
| **採檢日 `DateSampled`** | 穩定，且與 R 後端模型輸入一致 ✅ |

---

## 三、改了哪三處

檔案：`index.js`（repo 根目錄）。行號以本次 commit 為準。

### 1. `getLimsPatientData` 接受基準日（約 13233）

```diff
-function getLimsPatientData(data) {
+function getLimsPatientData(data, dateSampled) {
   const dob = syncLocalTime2UTC(data.DateOfBirth).getTime();
-  const ageString = computeAgeByDOB(dob);
+  const sampledTime = dateSampled ? syncLocalTime2UTC(dateSampled).getTime() : NaN;
+  const ageString = computeAgeByDOB(dob, isNaN(sampledTime) ? void 0 : sampledTime);
```

`computeAgeByDOB(birthDate, endDate, flag)` **本來就支援傳入基準日**，只是沒有任何呼叫端傳過。函式本體未修改。

### 2. `Framework` 供給採檢日（約 13281）

```diff
-    patient: getLimsPatientData(limsDataJson.patient),
+    patient: getLimsPatientData(limsDataJson.patient, limsDataJson.sample && limsDataJson.sample.DateSampled),
```

`limsDataJson.sample` 與 `limsDataJson.patient` 在同一層，`getLimsSampleData` 早已在讀 `DateSampled`，資料現成。

### 3. 健康動態追蹤的歷次年齡改用採檢日（約 53052）

```diff
-  computeAgeByDOB(…patient.dateOfBirth…, syncLocalTime2UTC(o.getDateReceived).toString())
+  computeAgeByDOB(…patient.dateOfBirth…, syncLocalTime2UTC(o.getDateSampled).toString())
```

改前趨勢圖用**收樣日**、本次結果用**今天**、模型輸入用**採檢日** —— 同一份報告裡三種基準。這處一併改成採檢日後三邊對齊。

> 註：`getHistoryFilteredSample()` 篩選歷史樣本時仍使用 `getDateReceived`，那是「挑哪些樣本」的邏輯，與年齡計算無關，**刻意未動**。

---

## 四、驗證

### 函式層級（DOB 1980-03-15、採檢日 2025-06-01、執行日 2026-08-19）

| | 實際年齡 |
|---|---|
| 改前（用今天） | 46.4 |
| **改後（用採檢日）** | **45.2** |
| R 後端同公式對照 | 45.213 ✅ 一致 |

### Fallback

`DateSampled` 為空字串 / `null` / `undefined` / 非法日期時，全部退回原本的「今天」行為，不會產生 `Invalid Date` 或 `NaN` 出現在報告上。

### 語法

`node --check` 通過（對照組：未修改的線上原檔也通過，確認檢查有效）。

### 實機

已在 LIMS 開報告驗證通過，四個套件別（MetaAge / MetaGuard / MetaPro / MetaCardio）無 console 錯誤。

---

## 五、副作用（需知會客服）

**既有報告重出後，實際年齡與差值會與當初印出去的不同。**

改後顯示的是「採檢當時的年齡」，這是正確值；但若客戶手上留有舊版報告，數字會對不上。舊報告印得越早、差異越大。

---

## 六、部署

改動只落在 `index.js` 這支 bundle。**R 後端（OmicsDP）與 `mProbeReports.pt` 都不需要動。**

部署路徑：

```
…/senaite/impress/browser/static/cdn/reports/meta-guard-tw/2.0.0/index.js
```

步驟：備份現行檔 → 覆蓋 → 重啟 Zope → 瀏覽器強制重整清快取 → 四個套件別各開一份報告驗證。

### 版本基底注意

本次以**從線上機器抓下來的實機檔**為基底修改，該檔與 `main:index.js` 的 blob 完全相同（`46c996f8`）。各功能分支（TMAO 等）都領先 `main`，**不可拿功能分支的 bundle 覆蓋線上**，會把未驗證的內容一起推上去。

### CRLF 注意

bundle 是 CRLF（每行皆有 CR）。手改插入新行時要補 `\r\n` 而非 `\n`。repo 設有 `core.autocrlf=true`，因此 `git show <ref>:index.js | md5sum` 與工作區檔案的 md5 **永遠不同**（即使 `git status` 顯示 clean）；比對版本請用 `git hash-object -- <file>` 對照 `git rev-parse <ref>:index.js`。

---

## 七、已知未處理項目

1. **`feat/tmao-profile` 分支尚未套用本修正。** 該分支的 `index.js` 仍是舊行為（用今天），TMAO 上線前必須補上這三處改動，否則會把此 bug 帶回線上。

2. **取位規則不一致。** 生理年齡走 `toRetain()`（`substring` 截斷，41.98 → 41.9），實際年齡走 `toFixed(1)`（四捨五入）。兩者相減的差值因此有最多 0.1 的系統性偏移。本次刻意未動。

3. **`computeAgeByDOB` 的餘數除數。** 先扣掉整數歲後，餘數除的是 365.25 而非當年實際天數（365 或 366），導致生日前一天算成 0.9993 而不是 1.0。影響 < 0.1 歲，未處理。
